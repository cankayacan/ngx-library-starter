import {Component, ViewContainerRef} from '@angular/core';
import {Ng2NotificationComponentsHelper, NotificationService} from 'ng2-notification';

@Component({
    selector: 'app',
    template: `
        <sample></sample>
    `
})
export class AppComponent {
}
